name: ci-staging

on:
  pull_request:
    branches:
      - staging

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Validate Files
        run: |
          ls
